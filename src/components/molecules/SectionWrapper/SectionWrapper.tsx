export interface SectionWrapperProps {
  /**
   * Add props here
   */
}

const SectionWrapper = ({}: SectionWrapperProps) => <div className="surface-primary rounded-3xl p-8">Testing</div>;

export default SectionWrapper;
